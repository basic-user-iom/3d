#include <versionPrecision>

// Depth-only fragment shader for external objects. External objects are flat
// shaded (no alpha-tested texture atlas), so the shadow pass just writes depth.
void main() {}
